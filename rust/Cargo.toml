[dependencies]
flutter_rust_bridge = "=2.11.1"

[dependencies.semaphore-bindings]
default-features = false
features = ["flutter"]
path = "/Users/zhengyawen/Documents/GitHub/semaphore-bindings"
version = "0.1.0"

[lib]
crate-type = [
    "cdylib",
    "staticlib",
]

[lints.rust.unexpected_cfgs]
check-cfg = ["cfg(frb_expand)"]
level = "warn"

[package]
edition = "2021"
name = "semaphore"
version = "0.1.0"

[workspace]
